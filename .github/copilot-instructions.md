# Project Instructions

- Frontend: React + TypeScript + Vite.
- Backend: Python + FastAPI.
- Keep RAG integrations behind small adapters; the local fallback must remain runnable without external services.
- Do not use LangChain or LlamaIndex for the initial implementation.
- Run backend tests with `cd backend; pytest` and frontend builds with `cd frontend; npm run build`.
